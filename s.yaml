edition: 1.0.0
name: 'fe-journey-faas' #  项目名称
access: 'aliyun' #  秘钥别名

vars:
  service:
    name: fe-journey-faas
    description: 'frontend journey faas service'
services:
  queryCaptcha:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: queryCaptcha # 函数名
        handler: authHttpService.queryCaptcha
        codeUri: '.'
        initializer: authHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /auth/queryCaptcha
              serviceName: ${vars.service.name}
              functionName: authHttpService-queryCaptcha
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  login:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: login # 函数名
        handler: authHttpService.login
        codeUri: '.'
        initializer: authHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /auth/login
              serviceName: ${vars.service.name}
              functionName: authHttpService-login
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  logout:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: logout # 函数名
        handler: authHttpService.logout
        codeUri: '.'
        initializer: authHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /auth/login
              serviceName: ${vars.service.name}
              functionName: authHttpService-logout
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  createAccount:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: createAccount # 函数名
        handler: userHttpService.createAccount
        codeUri: '.'
        initializer: userHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /user/create
              serviceName: ${vars.service.name}
              functionName: userHttpService-createAccount
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  getUserInfo:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getUserInfo # 函数名
        handler: userHttpService.getUserInfo
        codeUri: '.'
        initializer: userHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /user/getUserInfo
              serviceName: ${vars.service.name}
              functionName: userHttpService-getUserInfo
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  saveInterview:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: saveInterview
        handler: interviewHttpService.saveInterview
        codeUri: '.'
        initializer: interviewHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /interview/save
              serviceName: ${vars.service.name}
              functionName: interviewHttpService-saveInterview
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  listInterviews:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: listInterviews
        handler: interviewHttpService.listInterviews
        codeUri: '.'
        initializer: interviewHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /interview/list
              serviceName: ${vars.service.name}
              functionName: interviewHttpService-listInterviews
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  deleteInterview:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: deleteInterview
        handler: interviewHttpService.deleteInterview
        codeUri: '.'
        initializer: interviewHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /interview/delete
              serviceName: ${vars.service.name}
              functionName: interviewHttpService-deleteInterview
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  updateInterviewStatus:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: updateInterviewStatus
        handler: interviewHttpService.updateInterviewStatus
        codeUri: '.'
        initializer: interviewHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /interview/status
              serviceName: ${vars.service.name}
              functionName: interviewHttpService-updateInterviewStatus
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  updateUserInfo:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: updateUserInfo
        handler: userHttpService.updateUserInfo
        codeUri: '.'
        initializer: userHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /user/updateUserInfo
              serviceName: ${vars.service.name}
              functionName: userHttpService-updateUserInfo
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  recordVisit:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: recordVisit
        handler: profileHttpService.recordVisit
        codeUri: '.'
        initializer: profileHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /profile/recordVisit
              serviceName: ${vars.service.name}
              functionName: profileHttpService-recordVisit
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  getVisitStats:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getVisitStats
        handler: profileHttpService.getVisitStats
        codeUri: '.'
        initializer: profileHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /profile/visitStats
              serviceName: ${vars.service.name}
              functionName: profileHttpService-getVisitStats
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getMemberOrders:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getMemberOrders
        handler: profileHttpService.getMemberOrders
        codeUri: '.'
        initializer: profileHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /profile/memberOrders
              serviceName: ${vars.service.name}
              functionName: profileHttpService-getMemberOrders
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getPdfOrders:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getPdfOrders
        handler: profileHttpService.getPdfOrders
        codeUri: '.'
        initializer: profileHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /profile/pdfOrders
              serviceName: ${vars.service.name}
              functionName: profileHttpService-getPdfOrders
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getNavList:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getNavList
        handler: articleHttpService.getNavList
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/navList
              serviceName: ${vars.service.name}
              functionName: articleHttpService-getNavList
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getArticleStats:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getArticleStats
        handler: articleHttpService.getArticleStats
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/stats
              serviceName: ${vars.service.name}
              functionName: articleHttpService-getArticleStats
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  toggleLike:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: toggleLike
        handler: articleHttpService.toggleLike
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/like
              serviceName: ${vars.service.name}
              functionName: articleHttpService-toggleLike
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  toggleBookmark:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: toggleBookmark
        handler: articleHttpService.toggleBookmark
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/bookmark
              serviceName: ${vars.service.name}
              functionName: articleHttpService-toggleBookmark
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  recordShare:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: recordShare
        handler: articleHttpService.recordShare
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/share
              serviceName: ${vars.service.name}
              functionName: articleHttpService-recordShare
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  getUserActions:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getUserActions
        handler: articleHttpService.getUserActions
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/userActions
              serviceName: ${vars.service.name}
              functionName: articleHttpService-getUserActions
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getUserLikes:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getUserLikes
        handler: articleHttpService.getUserLikes
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/userLikes
              serviceName: ${vars.service.name}
              functionName: articleHttpService-getUserLikes
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getUserBookmarks:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getUserBookmarks
        handler: articleHttpService.getUserBookmarks
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/userBookmarks
              serviceName: ${vars.service.name}
              functionName: articleHttpService-getUserBookmarks
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  recordArticleView:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: recordArticleView
        handler: articleHttpService.recordArticleView
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/view
              serviceName: ${vars.service.name}
              functionName: articleHttpService-recordArticleView
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  batchArticleStats:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: batchArticleStats
        handler: articleHttpService.batchArticleStats
        codeUri: '.'
        initializer: articleHttpService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /article/batchStats
              serviceName: ${vars.service.name}
              functionName: articleHttpService-batchArticleStats
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getAlgoProblems:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getAlgoProblems
        handler: algorithmHTTPService.getProblems
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/problems
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-getProblems
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getAlgoProblemDetail:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getAlgoProblemDetail
        handler: algorithmHTTPService.getProblemDetail
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/problem/detail
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-getProblemDetail
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getAlgoTags:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getAlgoTags
        handler: algorithmHTTPService.getTags
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/tags
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-getTags
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  runAlgoCode:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: runAlgoCode
        handler: algorithmHTTPService.runCode
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 120
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/run
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-runCode
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  submitAlgoCode:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: submitAlgoCode
        handler: algorithmHTTPService.submitCode
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 120
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/submit
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-submitCode
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  getAlgoSubmissions:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getAlgoSubmissions
        handler: algorithmHTTPService.getSubmissions
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/submissions
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-getSubmissions
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  saveAlgoProblem:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: saveAlgoProblem
        handler: algorithmHTTPService.saveProblem
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/problem/save
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-saveProblem
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  deleteAlgoProblem:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: deleteAlgoProblem
        handler: algorithmHTTPService.deleteProblem
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/problem/delete
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-deleteProblem
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  saveAlgoTestCases:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: saveAlgoTestCases
        handler: algorithmHTTPService.saveTestCases
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/testcase/save
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-saveTestCases
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  saveAlgoTag:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: saveAlgoTag
        handler: algorithmHTTPService.saveTag
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/tag/save
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-saveTag
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  importAlgoProblems:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: importAlgoProblems
        handler: algorithmHTTPService.importProblems
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 120
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/import
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-importProblems
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  saveAlgoDraft:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: saveAlgoDraft
        handler: algorithmHTTPService.saveDraft
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/draft/save
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-saveDraft
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  getAlgoDraft:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getAlgoDraft
        handler: algorithmHTTPService.getDraft
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/draft
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-getDraft
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getAlgoLastResult:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getAlgoLastResult
        handler: algorithmHTTPService.getLastResult
        codeUri: '.'
        initializer: algorithmHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /algorithm/lastResult
              serviceName: ${vars.service.name}
              functionName: algorithmHTTPService-getLastResult
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getBookList:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getBookList
        handler: bookHTTPService.getBookList
        codeUri: '.'
        initializer: bookHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /book/list
              serviceName: ${vars.service.name}
              functionName: bookHTTPService-getBookList
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  getBookDetail:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getBookDetail
        handler: bookHTTPService.getBookDetail
        codeUri: '.'
        initializer: bookHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /book/detail
              serviceName: ${vars.service.name}
              functionName: bookHTTPService-getBookDetail
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  createBookOrder:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: createBookOrder
        handler: bookHTTPService.createBookOrder
        codeUri: '.'
        initializer: bookHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /book/order/create
              serviceName: ${vars.service.name}
              functionName: bookHTTPService-createBookOrder
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  confirmBookOrder:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: confirmBookOrder
        handler: bookHTTPService.confirmBookOrder
        codeUri: '.'
        initializer: bookHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /book/order/confirm
              serviceName: ${vars.service.name}
              functionName: bookHTTPService-confirmBookOrder
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  getBookOrders:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: getBookOrders
        handler: bookHTTPService.getBookOrders
        codeUri: '.'
        initializer: bookHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /book/orders
              serviceName: ${vars.service.name}
              functionName: bookHTTPService-getBookOrders
      triggers:
        - name: http
          type: http
          config:
            methods:
              - GET
            authType: anonymous
  saveBook:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: saveBook
        handler: bookHTTPService.saveBook
        codeUri: '.'
        initializer: bookHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /book/save
              serviceName: ${vars.service.name}
              functionName: bookHTTPService-saveBook
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
  deleteBook:
    component: devsapp/fc
    props:
      region: cn-hangzhou
      service: ${vars.service}
      function:
        name: deleteBook
        handler: bookHTTPService.deleteBook
        codeUri: '.'
        initializer: bookHTTPService.initializer
        runtime: nodejs16
        memorySize: 4096
        cpu: 1.5
        diskSize: 512
        timeout: 60
        instanceConcurrency: 200
      customDomains:
        - domainName: auto
          protocol: HTTP
          routeConfigs:
            - path: /book/delete
              serviceName: ${vars.service.name}
              functionName: bookHTTPService-deleteBook
      triggers:
        - name: http
          type: http
          config:
            methods:
              - POST
            authType: anonymous
